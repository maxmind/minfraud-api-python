language: python

python:
  - 2.6
  - 2.7
  - 3.3
  - 3.4
  - nightly
  - pypy

install:
 - pip install pylint

script:
  - python setup.py test
  - if [[ $TRAVIS_PYTHON_VERSION == '2.7' ]]; then ./.travis-pylint.sh; fi

notifications:
  email:
    recipients:
      - dev@maxmind.com
    on_success: change
    on_failure: always
